# Copy values into Streamlit Community Cloud secrets or an ignored
# .streamlit/secrets.toml. Never commit real values.
PLATE_READER_ENV = "production"
PLATE_READER_STORAGE_MODE = "cloud"
PLATE_READER_WRITES_ENABLED = "true"
TURSO_DATABASE_URL = "libsql://REPLACE_WITH_DATABASE.turso.io"
TURSO_AUTH_TOKEN = "REPLACE_WITH_DATABASE_TOKEN"

# Use only when Streamlit Community Cloud has private email access enabled.
# Every database action is attributed to this one audit identity.
PLATE_READER_CLOUD_IDENTITY_MODE = "hosted"
PLATE_READER_HOSTED_USER_EMAIL = "REPLACE_WITH_OWNER_EMAIL"
PLATE_READER_HOSTED_USER_ROLE = "admin"

# Optional alternative for distinct per-user identities and roles:
# PLATE_READER_CLOUD_IDENTITY_MODE = "oidc"
# PLATE_READER_OIDC_PROVIDER = "google"
# [auth]
# redirect_uri = "https://REPLACE_WITH_APP_HOST/oauth2callback"
# cookie_secret = "REPLACE_WITH_RANDOM_SECRET"
#
# [auth.google]
# client_id = "REPLACE_WITH_GOOGLE_CLIENT_ID"
# client_secret = "REPLACE_WITH_GOOGLE_CLIENT_SECRET"
# server_metadata_url = "https://accounts.google.com/.well-known/openid-configuration"

# Use this instead of auth.google when Microsoft Entra is selected.
# [auth.microsoft]
# client_id = "REPLACE_WITH_MICROSOFT_CLIENT_ID"
# client_secret = "REPLACE_WITH_MICROSOFT_CLIENT_SECRET"
# server_metadata_url = "https://login.microsoftonline.com/REPLACE_TENANT/v2.0/.well-known/openid-configuration"
